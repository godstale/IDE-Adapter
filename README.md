# IDE-Adapter
IDEA (Integrated Development Environment Adapter) is a VS Code extension that exposes IDE features — such as those in VS Code, Cursor, and Windsurf — to CLI-based tools like Claude CLI.
